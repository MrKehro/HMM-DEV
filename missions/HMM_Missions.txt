HMM_German_Minor_Missions = {
	slot = 5
	generic = no
	ai = yes
	potential = {
		has_country_flag = hmm_german_minor_missions
		NOT = { map_setup = map_setup_random }
	}

	has_country_shield = yes

	hmm_german_minor_berlin = {
		icon = mission_conquer_1_province
		required_missions = { }
		position = 1

		provinces_to_highlight = {
			province_id = 50
			NOT = { owned_by = ROOT }
		}

		trigger = {
			owns = 50 # Berlin
		}

		effect = {
			add_prestige = 5
		}
	}

	hmm_german_minor_surpass_austria = {
		icon = mission_urbanization
		required_missions = { }
		position = 2

		trigger = {
			exists = HAB
			HAB = {
				is_subject = no
			}
			total_development = HAB
			NOT = { HAB = { total_development = ROOT } }
		}

		effect = {
			add_adm_power = 50
		}
	}
}